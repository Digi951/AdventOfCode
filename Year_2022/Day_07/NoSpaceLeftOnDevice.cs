using System;

namespace AdventOfCode.Year_2022.Day_07;

public static class NoSpaceLeftOnDevice
{
    public static Int32 Calculate(List<String> inputs)
    {
        var instructions = inputs.Select(x => x.Split(' ')).ToList();
        var result = 0;

        return result;
    }
}
